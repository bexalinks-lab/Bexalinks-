import AdminSupport from '../../../frontend/AdminSupport';

export const metadata = { title: 'Support tickets — Bexalink' };

export default function AdminSupportPage() {
  return <AdminSupport />;
}
